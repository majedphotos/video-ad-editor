---
name: video-ad-editor
description: يمنتج فيديو كلام للكاميرا إلى ريل/إعلان 9:16: يشيل السكتات، كابشن عربي بتوقيت الكلمة، وموشن قرافيكس بألوانه. وفيه مونتاج مقاطع بلا كلام وبودكاست بكاميرتين. Use when user says "منتج هذا المقطع"، "سو من هذا الفيديو إعلان"، "شيّل السكتات"، "حوّل الفيديو لريل"، "ركّب كابشن عربي"، "اقص الصمت"، "مونتاج من هالمقاطع"، "بودكاست"، "كاميرتين"، or sends a talking video/clip folder. Not for carousels or video covers.
---

# مونتاج إعلان الفيديو — بلا برنامج مونتاج

المونتاج كله كود: ffmpeg يقص، وِسبر يفرّغ بتوقيت الكلمة، ومحرّك رسم يركّب الكابشن والموشن قرافيكس فوق الفيديو. المخرَج MP4 واحد جاهز للنشر.
مو للكاروسيل (carousel-creator) ولا لكفر الفيديو (animated-video-cover).

## ثلاثة أوضاع — اقرأ المدخل وامشِ

| | **إعلان كلام** (الافتراضي) | **مونتاج مقاطع** | **بودكاست** |
|---|---|---|---|
| المدخل | فيديو واحد فيه شخص يتكلم | مجلد مقاطع بلا كلام (مقهى · سفرة · منتج) | كاميرتان أو أكثر لنفس الجلسة |
| الاختيار على | الكلام (السكتات والجمل المعادة) | المشهد (وضوح · حركة · إضاءة) | من يتكلم + الزاوية |
| كابشن؟ | نعم، متزامن بالكلمة | ⛔ لا — ولا تفريغ | نعم، كلمتين-ثلاث |
| الخطوات | 0-11 تحت | `references/montage.md` | `references/podcast.md` |

⛔ **لا تسأله «أي وضع تبي؟»** — قل له بجملة وحدة شنو فهمت وكمّل.

**المحرّك:** دائماً الخفيف (`04_render_frames.js`) وبلا ما تذكر كلمة «محرّك». شاشة التعديل الحية (ريموشن) تنفتح بس إذا طلب هو يعدّل بنفسه → `references/remotion.md`.

---

## ⛔ أسلوبك مع المستخدم

**أنت اللي تجهّز، مو هو.** ممنوع تعطيه قائمة أوامر ينفّذها.
- **لا تسأله «عندك جذيه؟»** — افحص بنفسك، وإذا ناقص شي: «ناقصني أداتين أجهّزهم لك الحين، ياخذ دقيقتين — أبدأ؟»
- **بلا مصطلحات** — «أداة ترسم الشرائح» لا «puppeteer»، «جودة الفيديو» لا «كوديك».
- **قل له وين احنا كل خطوة** وقدّر الوقت: «شلت السكتات — نصف الفيديو راح. الحين أفرّغ كلامك، 3 دقايق.»
- **شي واحد بكل مرة.** و**لا تسلّم بلا ما تريه** لقطة أو ملخصاً بعد كل مرحلة مهمة.

## ⛔ وين يشتغل؟ على جهازه فقط
عبر **Claude Code على الجهاز** (ماك، وويندوز من v2.8): يحتاج ffmpeg وموديل وِسبر ومتصفحاً للرسم. **داخل تطبيق كلود بالسحابة ما يشتغل** (تنزيل الموديل محجوب وما فيه متصفح) — الجواب: «افتح Claude Code على جهازك واختر Local وشغّل نفس الأمر». `00_setup.sh` يكتشف هذا.
**ويندوز:** `python` مو `python3`، والمسارات `C:/…` — `references/windows.md`.

## الخطوة 0 — جهّز بصمت
```bash
bash scripts/00_setup.sh
```
- «كل شي جاهز» → لا تذكر الموضوع.
- «🆕 فيه إصدار أحدث» → «فيه تحديث للسكل — أحدّثه لك الحين؟» وبعد موافقته `bash scripts/00_setup.sh --update`.
- ناقص → قل له بجملة شنو راح تنزّل، خذ موافقته، ثم `bash scripts/00_setup.sh --install`. لو فشل شي قل الحل بجملة بشرية بلا لصق الخطأ.

جهّز مجلد شغل، وانسخ له `scripts/compose.REFERENCE.html` باسم `compose.html`.

---

## الخطوة 1 — هويته البصرية (ما فيه لون افتراضي)
⛔ **ممنوع تفترض الكريمي والطيني** (ذاك ثيم كلود). ⛔ **ولا تغيّر ثيماً سابقاً** — لو عنده `theme.json` من فيديو قبل، استخدمه كما هو؛ تغيير لون أساسي بطلب صريح منه فقط.

1. دوّر `.auto-memory/creator-profile.md` — إن وُجد خذ الألوان والخط والحساب: «راح أمشي بألوانك: كذا وكذا — تمام؟»
2. ما فيه؟ **سؤال واحد**: «شنو ألوان حسابك؟ عطني لون الخلفية ولون التمييز وشعارك (أو رابط حسابك وأنا أطلّعها).»
3. اكتب `<work>/theme.json` وانسخ شعاره `logo.png`:
```json
{ "bg":"#101828", "ink":"#F5F7FA", "acc":"#F2B33D", "clay":"#C98B18", "mut":"#98A2B3",
  "font":"Tajawal", "handle":"@his_handle", "logo":"logo.png", "grade": false,
  "faceAnchor": 0.30, "badgeUntil": 0, "capStyle": "card" }
```
- كل الرسم يشتق ألوانه من هذي القيم (الكروت والظلال ولون النص فوق التمييز). غامق أو فاتح، الاثنين يشتغلون.
- **شارة الحساب مطفية** (`badgeUntil: 0` الافتراضي) — الاسم بالمنصة نفسها وبكرت النهاية. `badgeUntil: 3` تعيدها أول 3 ثوانٍ لو طلبها.
- `grade` مطفي — شوف القاعدة 4 تحت. `capStyle` وحركات الكلمة: `references/word-fx.md`. `"pace":"calm"`: `references/explainer-and-pace.md`.

## الخطوة 2 — خذ منه الفيديو
| الطريقة | شلون |
|---|---|
| **ملف على جهازه** ← الأفضل | انسخه `<work>/src.mov` |
| **رابط قوقل درايف** | مشاركة «أي شخص لديه الرابط»، ثم `curl -sL "https://drive.usercontent.google.com/download?id=<ID>&export=download&confirm=t" -o <work>/src.mov` (الـID بين `/d/` و`/view`) |
| **كونكتور قوقل درايف** | لو الملف خاص — أدوات الكونكتور بالجلسة |

4K أفضل (الزوم يُقص من الأصل)، بس 1080p يشتغل — **لا ترفضه ولا تطلب إعادة تصوير.**
تحقق: `ffprobe -v error -show_entries format=duration -of csv=p=0 <work>/src.mov`

---

## الخطوات 3-11 — الإنتاج

### 3) خطة القص
```bash
python3 scripts/01_cut_plan.py <work>
```
قل له كم انشال: «شلت 52 ثانية سكوت — الفيديو صار 46 بدل 98.»
**3.5) حسّن الصوت (اختياري):** صدى أو ضجيج، أو قال «حسّن الصوت» → `references/audio-enhance.md`.

### 4) التفريغ بتوقيت الكلمة
```bash
ffmpeg -v error -i <work>/src.mov -vn -ac 1 -ar 16000 -y <work>/a.wav
python3 -m whisper <work>/a.wav --language ar --model medium --word_timestamps True \
  --output_format json --output_dir <work> --fp16 False --verbose False > <work>/whisper.log 2>&1
```
ياخذ دقايق — شغّله بالخلفية وقل له كم يتوقع. لو فشل: `tail -5 <work>/whisper.log`.

### 5) التصحيح والكابشن
اقرأ `a.json`، صحّح كل جملة (وِسبر يغلط بالعامية الخليجية)، واكتب `<work>/fixes.json`:
```json
{ "fix": [["كل","شي","تشوفه"], ["الكابشن","الزوم"]], "hot": ["تشوفه","الزوم"], "fx": {"خلاص":"drop"} }
```
`hot` = كلمات تنحبس بقرص التمييز وقت نطقها · `fx` اختياري (`references/word-fx.md`). **عدد كلمات كل جملة = عدد كلمات وِسبر لنفس الجملة** وإلا تختل التوقيتات (السكربت يوقفك).
```bash
python3 scripts/02_captions.py <work>
```

### 5.5) اعرض عليه النص — وشيل اللي ما عجبه ← إلزامي، قبل تصميم أي مشهد
```bash
python3 scripts/10_script_edit.py <work> show
```
يطبع كلامه مرقّماً بالتوقيت، ويكتب `script.txt`، **ويطبع الجمل المعادة واقتراح الحذف بنفس المرة** — لا تشغّل `dupes` بعده.
⛔ **اطبع السكربت كاملاً بالشات** وقل: «شنو تبي أشيل؟» — لا تكتفِ بالملف على القرص.
**الجملة المعادة:** الأولى هي الغلط والثانية التصحيح — «قلت الجملة مرتين — ‹قدرنا نحل السبب› ثم ‹قدرنا نعرف السبب›. أشيل الأولى؟»
```bash
python3 scripts/10_script_edit.py <work> drop 6 8       # يشيل الجملتين من الفيديو والصوت
python3 scripts/10_script_edit.py <work> keep 1 2 5 9   # يبقي هذي بس
python3 scripts/10_script_edit.py <work> undo
```
كل اللي بعدها ينزاح، و`cut.json` و`caps.json` و`sfx.json` تتحدّث. **سوّها هني** — الحذف بعد تصميم المشاهد يزيح أوقاتها كلها. وبعد أي حذف: أعد الخطوة 6، والرسم بـ`--force`.

### 6) القص والزوم
```bash
python3 scripts/03_cut_zoom.py <work>
mkdir -p <work>/vfr && ffmpeg -v error -i <work>/cutz.mp4 -vf fps=30 -q:v 3 -y <work>/vfr/%05d.jpg
```

### 7) صمّم المشاهد ← أهم خطوة

**البنية جاهزة بـ`compose.html`، لا تلمسها:** الفيديو بالأوضاع (بانتقال ناعم)، الكابشن وتظليل الكلمة المنطوقة، ورا الراس، البي-رول، المشاهد المولّدة، الملصقات، شريط التقدّم، كرت النهاية، واشتقاق الألوان. **افتراضياً ما يرسم أي مشهد مخصص** — الفيديو ملء الشاشة + كابشن + كرت النهاية.

**مشاهدك أنت تكتبها بآخر `compose.html`** بسكربت ثاني تحت سكربت المحرّك:
```html
<script>
SCENES=[{s:0,e:4.1,m:R_FULL},{s:4.1,e:9.8,m:R_DOWN,gb:480},{s:9.8,e:99,m:R_FULL}];   // أو studio.json ← scenes
function listPanel(t){ if(t<4.1||t>9.8) return; panelIn(t,4.1,9.8,()=>{ /* رسم بإحداثيات 130..950 × 278..458 */ }); }
SCENE_LIST.push(['listPanel',listPanel]);            // بدون التسجيل ما ينرسم
</script>
```
- كل مشهد ياخذ `t` ويلتصق بتوقيت الكلمة من `caps.json` (`wordsOf(i)` = كلمات الجملة i، العدّ من صفر) — لا بتوقيت تقريبي.
- مشهد يشتغل على صورة الفيديو نفسها (مثل القليتش) ينرسم بطبقة الفيديو: `SCENE_LIST.push(['myFx',myFx,'video'])`.
- من الاستوديو: `studio.json` ← `"sceneFx":["listPanel"]` يسجّل بالاسم.
- مشهد يسقط يتخطّى ويطلع بالطرفية `[compose] ⚠️ مشهد متخطّى: …` — صلّحه، لا تتجاهله.
- أمثلة جاهزة للقراءة: `scripts/compose.EXAMPLES.js` (ما يتحمّل افتراضياً — راسه يشرح شلون).

**اللي تبتكره:** اعصف 3-4 أفكار لكل جملة، والفكرة **استعارة بصرية لما يقوله**، لا زينة:

| يقول | المشهد |
|---|---|
| يعدّد أشياء | بطاقات تدخل وحدة مع كل كلمة ثم تنقلب بعلامة صح |
| رقم أو سعر | عدّاد يلف وينزل على الرقم بضربة |
| «انكسر / خطأ» | قليتش يزحزح شرائح الصورة + شقوق |
| مشكلة ثم إصلاح | كرت الفيديو يسوَدّ بتحذير ← شريط تقدّم + قائمة تتعلّم بصح |
| «ملف واحد» | كرت ملف وبطاقات تطير وتندمج فيه |
| دعوة للتعليق | مربع تعليق والكلمة تنكتب حرف حرف |

**⛔ قاعدة التخطيط (معتمدة من المستخدم):**

| اللحظة | المستطيل | الشكل |
|---|---|---|
| كلام بلا رسم | `R_FULL` | وجهه ملء الشاشة، أسفل الكابشن عند 1460 |
| أي رسم أو موشن | `R_DOWN` (مرن) | الرسم فوق (y 280-520) ← الكابشن ← وجهه تحت بعرض الشاشة كامل |
| بي-رول أو لوحة كبيرة | `R_LOWER` | الكرت الكبير فوق ← الكابشن ← وجهه صغيراً تحت |
| شرح كامل (أرقام، قوائم) | `R_OFF` | بلا وجه، صوته يكمّل — `references/explainer-and-pace.md` |

- ⛔ **الكابشن ما ينكتب على وجه المتحدث أبداً.** بأي وضع فيه كرت فيديو، الكابشن كله فوق الكرت (بكل الستايلات)، وشريط التقدّم معه — المحرّك يحسبها من مستطيل المشهد.
- **`R_DOWN` كرت مرن:** يصغر حسب أسفل الرسم (`gb`) وعدد أسطر الكابشن، فالرسم والكابشن ما يتزاحمان. اللوحات داخل `panelIn()` وهي تتكبّر 1.2 وحدها.
- **لا تفرط بـ`R_DOWN`:** الهوك (أول 3-4 ثوانٍ) ملء الشاشة دائماً · جملة فيها ذروة أو انفعال أو سؤال → ملء الشاشة · لا تتجاوز نصف مدة الفيديو، ولا أكثر من **8 ثوانٍ** متواصلة بلا لقطة ملء شاشة · ما فيه رسم؟ ملء الشاشة.
- «ملء الشاشة» يُعرف بالمساحة (`isFull()`) لا بالزوايا.
- `{s,e,m,nocap:true}` = مشهد بلا كابشن (لما اللوحة فيها النص نفسه).
- شبكة خفيفة بالخلفية كل 60px (`grid:false` بـtheme.json يوقفها).

**بي-رول (اختياري):** فيديو ثانٍ من الشركة؟ استخرج المفيد فريمات `<work>/broll/<اسم>_%04d.jpg`، عرّف مداه بـ`BR_NEED`، واعرضه بـ`brCard()` مع `R_LOWER` (`BRCROP` يقصّ الترجمة المحروقة). 3-4 لقطات بالفيديو تكفي. `R_LOWER` يدخل حزام انستقرام عن قصد — مسموح لأن المغطّى صورة لا نص.

**⛔ المنطقة الآمنة** (أزرار انستقرام — `08_safe_check.js` يفحصها):

| المنطقة | لا تحط فيها نصاً |
|---|---|
| أعلى | y 0-150 |
| أسفل | y 1620-1920 (وحزام حذر 1500-1620) |
| يمين | x 950-1080 بين y 1100 و1750 (لايك · تعليق · مشاركة) |

المحرّك مضبوط أصلاً: أسفل الكابشن 1460 وشريط التقدّم 1492 بملء الشاشة. **وأول كابشن يبان بأول نصف ثانية.**

**عاين قبل ما ترسم كل شي — ورقة وحدة بـ6 لقطات:**
```bash
node scripts/04_render_frames.js <work> preview 1.2 4.6 12.3 27.6 31.0 48.4
bash scripts/07_contact_sheet.sh <work> <work>/sheet.jpg 1.2 4.6 12.3 27.6 31.0 48.4
```
الورقة 3 أعمدة × صفّين بعرض 1080 وعلى كل لقطة توقيتها. **اقرأ `sheet.jpg` وحدها** — لا الصور فرادى، ولا أوراق متعددة. اعرضها عليه قبل الرسم الكامل.

### 7.x) إضافات — كل وحدة بمرجعها، لا تفتحه إلا لما تحتاجه
- **تسجيل شاشة من الجوال** → `references/screen-recording.md` (النسبة الأصلية داخل إطار جوال مرسوم، `R_OFF`، كت مباشر، علامة الضغط).
- **أنماط القصّ الثلاثة** (الكلام ورا الشخص · واقف قدام اللوحة · راسك برّا المستطيل — ماك) → `references/cut-styles.md`.
- **حركات الكلمة وستايلات الكابشن والكشيدة** → `references/word-fx.md`.
- **شرح كامل `R_OFF` + إيقاع هادي** → `references/explainer-and-pace.md`.
- **خلفية باهتة** (`14_backdrop.py <work> 0.62`) → `references/backdrop.md`.
- **كولاج** (الشاشة ورقة وهو قصاصة، بلا كابشن) → `references/collage.md` — اقرأه قبل `13_collage.js`.
- **مشاهد مولّدة بالذكاء الاصطناعي** (صورة/مقطع من كلامه والمحرّك يرسم فوقه، 2-4 بالفيديو، يحتاج `FAL_KEY`) → `references/gen-scenes.md`.

### 8) المؤثرات الصوتية
```json
{ "outro": 5.2, "whoosh_up": [3.1,11.25], "whoosh_down": [7.85], "thud": [27.27,29.47], "tap": [23.08,24.06] }
```
```bash
python3 scripts/05_sfx.py <work>
```

### 9) الرسم النهائي والتجميع
```bash
node scripts/04_render_frames.js <work> all          # يكمّل من وين وقف — ما يعيد فريماً جاهزاً
bash scripts/06_encode.sh <work> <work>/ad-final.mp4
```
عدّلت مشهداً بعد الرسم؟ أعد نافذته بس: `node scripts/04_render_frames.js <work> range 26.4 31.2` ثم جمّع. (`--force` مع `all` يعيد الكل؛ السكربت ينبّهك لو بقي فريم ناقص.)

### 10) معايرة الصوت (+ ملف صوتي بالخلفية)
```bash
bash scripts/06b_master.sh <work> <work>/ad-final.mp4 <work>/ad-master.mp4
```
يرفع الصوت لـ‎-14 LUFS (نفس علو الفيد) ويطبع سطراً واحداً بالعلو والذروة. لو فيه `<work>/bg-audio.mp3` ينضاف وينخفض تلقائياً كل ما يتكلم. الصورة تُنسخ بلا إعادة ترميز.

### 11) ملف الترجمة
```bash
python3 scripts/09_srt.py <work> ad-master
```
`.srt` (يوتيوب ولينكدإن) و`.txt` = نص كلامه كامل لكابشن البوست.

---

## ⛔ القواعد الثابتة
1. **المؤثر الصوتي بلحظة لها معنى** (رقم ينزل، شي ينكسر، مشهد ينتقل) — مو رشّ على كل كلمة. 15 حدثاً بالدقيقة بالكثير، الذروة تحت ‎-18 dBFS، واعرض عليه وين حطّيتها قبل التصدير.
2. **أرقام غربية دائماً** (0-9).
3. **الألوان من `theme.json` لا غير** — لا لون ثابت بالكود.
4. **صفر تعديل لوني على صورته** — لا تدرّج ولا صبغة ولا LUT ولا بلر (`grade:false`)، إلا بطلب صريح منه. الألوان للكروت والنصوص فقط.
5. **آيفون HDR → محوّل أبل إلزامياً** (`03_cut_zoom.py` يكشفه ويبني `hdr2sdr.swift`) — التحويل الساذج يبهّت الصورة فيحسبها «تعديلاً لونياً».
6. **صحّح تفريغ وِسبر** قبل الكابشن، و**لا تخترع كلاماً ما قاله** — لو ما تعرف شي، عمّم بدل ما تخمّن.
7. **لا نشر ولا جدولة** — التسليم ملف بس.
8. **«ورا الشخص» وأنماط القصّ مرة أو مرتين بالفيديو** — أكثر يفقد أثره.
9. **سمّها «ملف صوتي بالخلفية» لا «موسيقى»** — المحتوى يحدّده هو، وانت تحطه بـ`bg-audio.mp3` كما هو.
10. **اخترع مشاهد جديدة كل مرة.** الأمثلة مكتبة أنماط، مو قالب يُنسخ.
11. **أي صورة أو تسجيل شاشة أو شعار بنسبته الأصلية** — تصغير/تكبير نسبي فقط، لا ضغط ولا قصّ إلا بطلب صريح.

## ✅ التحقق قبل التسليم (إلزامي)
0. **المنطقة الآمنة والهوك** — آلي، ما يحتاج عينك:
```bash
node scripts/08_safe_check.js <work> --shot
```
يرسم كل لحظة بنفس حمولة الرسم الحقيقي (theme + studio + behind) مرتين بلونين مكان الفيديو والبي-رول، واللي ما يتغيّر = رسمك — فيعدّ نصوصك داخل مناطق الأزرار ويتأكد إن أول كابشن قبل نصف ثانية. لقطات بلا فيديو (`R_OFF`، كرت النهاية) تنقاس كاملة. يخرج بالرمز 3 لو فيه خرق، و`--shot` يطلّع `safe.jpg` (540 عرض) **بس لو فشل**. الحدود تتعدّل بـ`<work>/safe.json`.
1. **المزامنة** — فرّغ الناتج وقارن بدايات الجمل بـ`caps.json`؛ الفرق أقل من 0.1 ثانية:
```bash
ffmpeg -v error -i <work>/ad-final.mp4 -vn -ac 1 -ar 16000 -y <work>/fa.wav
python3 -m whisper <work>/fa.wav --language ar --model small --output_format json --output_dir <work> --fp16 False --verbose False > <work>/whisper.log 2>&1
```
2. **الصوت** — سطر `06b_master.sh` الأخير: ≈ ‎-14 LUFS والذروة ‎-1.5 dBTP أو أقل.
3. **العين** — ورقة وحدة بـ6 لقطات من `ad-final.mp4`، تُشاف فعلاً.
4. **الحجم** — تحت 30 ميقا.

## 💰 توفير التوكنز
قياس 10 سبتمبر: فيديو 36 ثانية كلّف 3.1 مليون توكن — 41٪ أوامر طرفية متفرقة، و8 صور معاينة أكلت نحو 900 ألف حرف.
1. **أمر واحد لكل مرحلة** — اجمع الخطوات المستقلة (استخراج الصوت + التفريغ + الفريمات) بأمر واحد بالخلفية وانتظر إشعاره.
2. **ورقة لقطات وحدة لكل معاينة** (6 لقطات، `07_contact_sheet.sh`) — وأي فحص ثالث بالأرقام (`08_safe_check.js` · المزامنة · LUFS)، لا بالعين.
3. **لا تقرأ ملفاً كاملاً:** `grep -n` للدالة، و`tail -5` للسجلات. السكربتات تطبع سطر ملخص واحد بدل عدّاد ffmpeg.
4. **المراجع عند الحاجة فقط** — لا تفتح `references/` إلا لما يوصلك المدخل لها.
5. **`theme.json` يُحفظ للعميل** — الفيديو الثاني يبدأ منه. واحفظ `caps.json` و`cut.json` — التعديل اللاحق بلا إعادة تفريغ.
6. **ردود قصيرة:** سطران وصورة. ⛔ لا تعرض صورة إلا لسؤال بصري ما ينجاوب بغيرها.

## التسليم
اعرض الملف مع: المدة، كم انشال من السكتات، عدد المشاهد، والعلو النهائي. وسلّم معه `.srt` و`.txt`. واذكر أنك ما نشرت شيئاً.

خريطة السكربتات والملفات: `references/script-map.md`.
